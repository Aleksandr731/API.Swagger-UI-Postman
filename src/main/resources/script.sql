SELECT * FROM student;
WHERE age BETWEEN 10 AND 20;

SELECT name FROM student;

SELECT * FROM student;
WHERE CustomerName LIKE '%a%';

SELECT * FROM student;
WHERE age<id;

SELECT * FROM student;
ORDER BY age;

